.DS_Store
preview*.png
*-check.png
*-preview.png

# PRIVATE — never publish. Contains bank details + home address.
admin/

# Design mockups: deploy the HTML for review, but keep the screenshots out
design-options/*.png
